# geneie-frontend

## Branches

`dev` is the production branch — it is the one that deploys. Branch off `dev`, and open
pull requests against `dev`. `main` exists but is not what ships; do not treat it as the
release target.
